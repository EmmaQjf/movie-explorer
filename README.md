# movie-explorer
html --  search bar, request button, displayrender container 
1.search bar to get the input value.  empty search bar after request
2. send request to AI 
3. get data back from the API and json it and display on a ul list 
4. clear the container when render the info next time.


css 
javasript 
DOM 
fetch data from api, return  if catch. function 
render function 
loading states 
error handling 